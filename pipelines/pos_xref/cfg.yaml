compare: 
  read:
    filepath_or_buffer: ${INCOMING}
    usecols: ['SoldToName', 'BillToCustomerZip', 'BillToCustomerState']

compare_against:
  - read:
      filepath_or_buffer: ${EXISTING}
      usecols: ['SoldToName']
      
  - read:
      filepath_or_buffer: ${TO_BE_REVIEWED}
      usecols: ['SoldToName']



