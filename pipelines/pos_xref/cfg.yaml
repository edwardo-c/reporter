db: ${DB}

# the latest sales file to load
raw_pos_sales: ${RAW_POS_SALES}

# raw sql queires 
sql_load_raw_pos: ${LOAD_RAW_POS_SQL}
sql_load_candidates: ${SQL_LOAD_CANDIDATES}
sql_decisions: ${DECISIONS_SQL}

# exports
candidates_out: ${CANDIDATES_OUT}
xref_out: ${XREF_OUT}
customers_out: ${CUSTOMERS_OUT}

decisions_path: ${DECISIONS_PATH}
decisions_sql: ${DECISIONS_SQL}